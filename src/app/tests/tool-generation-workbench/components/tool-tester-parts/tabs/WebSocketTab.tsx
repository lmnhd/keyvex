"use client";

import React from "react";
import WebSocketDebugConsole from "../websocket-debug-console";

// No props currently required; leaving placeholder for future extension.
const WebSocketTab: React.FC = () => {
  return <WebSocketDebugConsole />;
};

export default WebSocketTab;


