// Temporary re-export shim while we migrate all code to the unified TCC types.
// Once every import is switched to "@/lib/types/tcc-unified", this file (and
// its usages) can be deleted.

export * from '../tcc-unified';
